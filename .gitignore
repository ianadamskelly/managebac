# Secrets — the top-level .env holds live ManageBac credentials; never commit
.env
.env.*
webapp/.env
webapp/.env.*
# …but do commit the example templates (no real secrets)
!.env.example
!webapp/.env.example

# OS
.DS_Store

# Dependencies / build (also covered by webapp/.gitignore)
node_modules/
.next/
out/
build/

# Local student/portfolio uploads
webapp/uploads/

# TypeScript build info
*.tsbuildinfo
