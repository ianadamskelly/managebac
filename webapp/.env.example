# PostgreSQL connection string
DATABASE_URL="postgresql://USER@localhost:5432/managebac_dev?schema=public"

# Secret used to sign session cookies — generate a random value, e.g. `openssl rand -base64 32`
AUTH_SECRET="change-me-to-a-long-random-string"
